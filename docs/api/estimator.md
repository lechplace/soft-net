# SoftEstimator

Abstract base class — inherited by `SoftClassifier` and `SoftRegressor`.

Dokumentuje wspólne metody: `fit`, `predict`, `score`, `summary`, `explain`, `from_preset`.

---

::: softnet.base.estimator.SoftEstimator
    options:
      show_source: true
      members:
        - __init__
        - fit
        - predict
        - score
        - summary
        - explain
        - from_preset
